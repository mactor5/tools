name: Deploy Docker Webtop

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Linux 发行版'
        required: true
        default: 'debian-kde	'
        type: choice
        options:
          - "debian-kde"
          - "arch-kde"
          - "ubuntu-kde"
          - "alpine-kde"
          - "fedora-kde"
          - "debian-xfce"
          - "arch-xfce"
          - "ubuntu-xfce"
          - "alpine-xfce"
          - "fedora-xfce"
          - "debian-mate"
          - "arch-mate"
          - "ubuntu-mate"
          - "alpine-mate"
          - "fedora-mate"
          - "debian-i3"
          - "arch-i3"
          - "ubuntu-i3"
          - "alpine-i3"
          - "fedora-i3"
          - "debian-openbox"
          - "arch-openbox"
          - "ubuntu-openbox"
          - "alpine-openbox"
          - "fedora-openbox"
          - "debian-icewm"
          - "arch-icewm"
          - "ubuntu-icewm"
          - "alpine-icewm"
          - "fedora-icewm"

jobs:
  Server:
    runs-on: ubuntu-latest
    steps:
      - name: 最大化磁盘空间
        uses: easimon/maximize-build-space@master
        with:
          overprovision-lvm: 'true'
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'

      - name: 迁出代码
        uses: actions/checkout@v3
        
      - name: 检查服务器配置
        id: check_server
        run: |
          echo -e "已知CPU型号(性能降序): 7763，8370C，8272CL，8171M，E5-2673\n"
          echo "--------------------------CPU信息--------------------------"
          echo "CPU物理数量: $(cat /proc/cpuinfo | grep 'physical id' | sort | uniq | wc -l)"
          echo "CPU核心数量: $(nproc)"
          echo -e "CPU型号信息:$(cat /proc/cpuinfo | grep -m1 name | awk -F: '{print $2}')\n"
          echo "--------------------------内存信息--------------------------"
          echo "已安装内存详细信息:"
          echo -e "$(sudo lshw -short -C memory | grep GiB)\n"
          echo "--------------------------硬盘信息--------------------------"
          echo "硬盘数量: $(ls /dev/sd* | grep -v [1-9] | wc -l)" && df -hT
          echo "--------------------------网络信息--------------------------"
          curl -s -4 ipinfo.io > raw_net_info
          cat raw_net_info | jq -r '
            "IP地址           \(.ip)\n" +
            "所在城市         \(.city)\n" +
            "所在区域         \(.region)\n" +
            "国家             \(.country)\n" +
            "地理位置         \(.loc)\n" +
            "组织             \(.org)\n" +
            "邮政编码         \(.postal)\n" +
            "所在时区         \(.timezone)\n"' 

          echo "public_ip=$(cat raw_net_info | jq -r '.ip')" >> $GITHUB_OUTPUT
          echo "city=$(cat raw_net_info | jq -r '.city')" >> $GITHUB_OUTPUT
          echo "region=$(cat raw_net_info | jq -r '.region')" >> $GITHUB_OUTPUT
          echo "country=$(cat raw_net_info | jq -r '.country')" >> $GITHUB_OUTPUT
          echo "loc=$(cat raw_net_info | jq -r '.loc')" >> $GITHUB_OUTPUT
          echo "org=$(cat raw_net_info | jq -r '.org')" >> $GITHUB_OUTPUT
          echo "postal=$(cat raw_net_info | jq -r '.postal')" >> $GITHUB_OUTPUT
          echo "timezone=$(cat raw_net_info | jq -r '.timezone')" >> $GITHUB_OUTPUT

      - name: 加入Tailscale私有网络
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:githubactions
          version: latest

      - name: 查看IP地址
        id: get_local_ip
        run: |
          ip a
          LOCAL_IP=$(ip -o -4 addr list | awk '/100\./ {print $4}' | cut -d/ -f1)
          echo "提取到本地 IP: ${LOCAL_IP}"
          echo "local_ip=${LOCAL_IP}" >> $GITHUB_OUTPUT

      - name: 解析 Linux 发行版并设置安装包
        id: set_install_packages
        run: |
          VERSION="${{ github.event.inputs.version }}"
          DISTRO=$(echo "$VERSION" | cut -d'-' -f1)
          case "$DISTRO" in
            arch)
              INSTALL_PACKAGES="noto-fonts-cjk"
              ;;
            debian|ubuntu)
              INSTALL_PACKAGES="fonts-noto-cjk"
              ;;
            alpine)
              INSTALL_PACKAGES="font-noto-cjk"
              ;;
            *)
              INSTALL_PACKAGES=""
              ;;
          esac
          
          echo "INSTALL_PACKAGES=$INSTALL_PACKAGES" >> $GITHUB_ENV

      - name: 部署 Docker Webtop
        run: |
          mkdir webtop
          cd webtop
          cat > docker-compose.yml << 'EOF'
          services:
            webtop:
              image: lscr.io/linuxserver/webtop:${{ github.event.inputs.version }}
              container_name: webtop
              security_opt:
                - seccomp:unconfined
              environment:
                - PUID=1000
                - PGID=1000
                - TZ=Etc/UTC
                - SUBFOLDER=/
                - TITLE=${{ github.event.inputs.version }}
                - DOCKER_MODS=linuxserver/mods:universal-package-install 
                - INSTALL_PACKAGES=${INSTALL_PACKAGES}
                - LC_ALL=zh_CN.UTF-8
              volumes:
                - ./config:/config
                - /var/run/docker.sock:/var/run/docker.sock
              ports:
                - 3000:3000
                - 3001:3001
              shm_size: "2gb"
              restart: unless-stopped
          EOF
          docker compose up -d

      - name: 发送 Telegram 通知
        run: |
          # 获取当前时间（UTC+8 时区，北京时间）
          current_time=$(date -u +"%Y-%m-%d %H:%M:%S" -d "8 hour")

          # 构造 Telegram 消息
          message=$(cat <<EOF
          <b>🚀 Linux GUI 部署成功 🎉</b>

          🖥️ <b>公网 IP:</b> <code>${{ steps.check_server.outputs.public_ip }}</code>  
          🏠 <b>本地 IP:</b> <code>${{ steps.get_local_ip.outputs.local_ip }}</code>  
          🏙️ <b>城市:</b> <code>${{ steps.check_server.outputs.city }}</code>  
          🌍 <b>区域:</b> <code>${{ steps.check_server.outputs.region }}</code>  
          🗺️ <b>国家:</b> <code>${{ steps.check_server.outputs.country }}</code>  
          📍 <b>地理位置:</b> <code>${{ steps.check_server.outputs.loc }}</code>  
          🏢 <b>组织:</b> <code>${{ steps.check_server.outputs.org }}</code>  
          📮 <b>邮政编码:</b> <code>${{ steps.check_server.outputs.postal }}</code>  
          ⏰ <b>时区:</b> <code>${{ steps.check_server.outputs.timezone }}</code>  

          🏠 <b>访问地址:</b> <code>http://${{ steps.get_local_ip.outputs.local_ip }}:3000</code>
          🐧 <b>Linux Tag:</b> <code>${{ github.event.inputs.version }}</code>

          🕒 <b>当前时间:</b> <code>$current_time</code>  
          EOF
          )

          # 进行 URL 编码
          message=$(echo "$message" | jq -sRr @uri)

          # 发送 Telegram 消息
          curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" \
               -d "chat_id=${{ secrets.TELEGRAM_TO }}" \
               -d "parse_mode=HTML" \
               -d "text=$message"

      - name: 启动SSH终端
        uses: mxschmitt/action-tmate@v3
